<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssg.chatroomapp.mapper.MemberMapper">
    <select id="selectAll">
        SELECT * FROM member
    </select>

    <select id="selectById" parameterType="int">
        SELECT * FROM member WHERE member_id = #{memberId}
    </select>

    <select id="login" parameterType="MemberLoginDTO" resultType="Member">
        SELECT * FROM member WHERE id = #{id} AND password = #{password}
    </select>

    <insert id="insert" parameterType="Member">
        INSERT INTO member(id, password, name, email) VALUES(#{id}, #{password}, #{name}, #{email})
    </insert>

    <update id="update" parameterType="Member">
        UPDATE member SET name=#{name}, password=#{password}, email=#{email}
        WHERE member_id=#{memberId}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM member WHERE member_id=#{memberId}
    </delete>

</mapper>
